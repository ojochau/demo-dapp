{
    "name": "demo",
    "version": "0.1.0",
    "engines": {
        "node": ">=18.0.0"
    },
    "devDependencies": {
        "@ijstech/cli": "0.1.5",
        "@ijstech/compiler": "0.1.4",
        "@ijstech/components": "0.1.16"
    },
    "scripts": {
        "build": "rm -rf dist && npx compiler",
        "serve": "npx cli serve",
        "test": "npm run build && npm run serve"
    }
}